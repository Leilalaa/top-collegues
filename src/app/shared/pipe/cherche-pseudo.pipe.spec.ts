import { CherchePseudoPipe } from './cherche-pseudo.pipe';

describe('CherchePseudoPipe', () => {
  it('create an instance', () => {
    const pipe = new CherchePseudoPipe();
    expect(pipe).toBeTruthy();
  });
});
